---
import PageLayout from '@layouts/PageLayout.astro';
interface Props {
  code: string | number;
}

const { code } = Astro.props;
---

<PageLayout title={`Error ${code}`}>
  <h1>{code}</h1>
  <slot />
</PageLayout>