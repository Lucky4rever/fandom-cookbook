<slot />

<style is:global>
  div.wrapper {
    display: grid;
    grid-template-columns: [large-start] 5% [big-start] 10% [normal-start] 70% [normal-end] 10% [big-end] 5% [large-end];
    height: max-content;
    width: 100%;
  }

  div.wrapper > * {
    grid-column: normal;
  }

  section.front.front {
    position: relative;
    grid-column: large;
    height: calc(100svh - var(--header-size));
    width: 100%;
  }

  img.logo {
    position: absolute;
    top: 75%;
    left: 50%;
    translate: -50% -50%;

    height: 40%;
    width: 100%;

    object-fit: contain;
  }

  img, picture {
    height: 100%;
    width: 100%;
    object-fit: cover;
  }
  
  div.desc {
    grid-column: normal;
    display: grid;
    grid-template-columns: [full-start] 0px [left-start] 2fr [left-end] 0fr [right-start] 1fr [right-end] 0px [full-end];
    align-items: center;
  }

  div.desc *:not(h1, h2, h3, p:has(img), :first-child) {
    grid-column: left;
  }

  div.desc *:where(h1, h2, h3, :first-child) {
    grid-column: full;
  }

  div.desc p:has(img) {
    grid-column: right;
  }

  div.desc img {
    width: 100%;
    height: auto;
    max-height: 50rem;
    object-fit: contain;
    grid-column: right;
  }

  a.recipe {
    width: 100%;
    height: inherit;
    color: inherit;
    overflow: hidden;
    padding: 2rem;

    display: flex;
    flex-direction: row;
    justify-content: space-between;
    transition: width 0.5s ease-in-out;

    text-decoration: none;

    &:hover {
      cursor: pointer;
    }
  }

  div.recipe {
    width: 100%;
    height: fit-content;
    margin: 0.7rem 0.1rem;

    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;

    background-color: var(--secondary);
    color: #fff;
    border-radius: 1rem;

    transition: transform 0.5s ease-in-out;

    &:hover {
      transform: scale(1.01);
    }
  }

  div.recipe-wrapper {
    margin: 1rem 0;
  }

</style>